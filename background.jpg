import streamlit as st

st.set_page_config(page_title="Gemini Chatbot", page_icon="🤖", layout="wide")

def add_bg_from_local(image_file):
    with open(image_file, "rb") as f:
        encoded_string = f.read().encode("base64") if hasattr(f.read(), 'encode') else f.read()

    st.markdown(
        f"""
        <style>
        /* Full page background image */
        .stApp {{
            background-image: url("data:image/jpg;base64,{encoded_string.decode()}");
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }}

        /* Dark overlay to improve readability */
        .stApp::before {{
            content: "";
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0, 0, 0, 0.6);
            z-index: 0;
        }}

        /* Make chat container text white and overlay chat bubbles */
        .css-1v0mbdj.e1fqkh3o3 {{
            color: white;
            position: relative;
            z-index: 1;
        }}

        /* Style chat bubbles */
        .stChatMessage > div {{
            background-color: rgba(0, 0, 0, 0.5) !important;
            border-radius: 10px;
            padding: 0.75rem;
        }}

        /* Style input box */
        .stTextInput>div>input {{
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
        }}
        </style>
        """,
        unsafe_allow_html=True
    )

add_bg_from_local('background.jpg')
